# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
#- script: echo Hello, world!
#  displayName: 'Run a one-line script'

#- script: |
#    echo Add other tasks to build, test, and deploy your project.
#    echo See https://aka.ms/yaml
#  displayName: 'Run a multi-line script'

- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Subscription'
    azureResourceManagerConnection: 'Subscription-GBS-RHE-POC-100m-EA-DEV-Test-RI(c9408de5-5684-4eb3-84c7-c33cda73ab5c)'
    subscriptionId: 'c9408de5-5684-4eb3-84c7-c33cda73ab5c'
    location: 'East US'
#   templateLocation: 'Linked Artifacts'
    csmFile: 'Basics/create_storage_acct02.json'
    csmParametersFile: 'Basics/create_storage_acct_parameters02.json'
    deploymentMode: 'Incremental'
    deploymentName: 'cicd_storageacct'
    deploymentOutputs: '$var=ConvertFrom-Json ''$(storageAccountName)'' $value=$var.storageAccountName.value Write-Host "##vso[task.setvariable variable=storageAccount;]$value"'